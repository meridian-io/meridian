---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterpoolautoscalers.meridian.io
spec:
  group: meridian.io
  names:
    kind: ClusterPoolAutoscaler
    listKind: ClusterPoolAutoscalerList
    plural: clusterpoolautoscalers
    singular: clusterpoolautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .spec.minReplicas
      name: Min
      type: integer
    - jsonPath: .spec.maxReplicas
      name: Max
      type: integer
    - jsonPath: .status.currentReplicas
      name: Current
      type: integer
    - jsonPath: .status.currentUtilization
      name: Utilization
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterPoolAutoscaler automatically adjusts a ClusterPool's replica count
          based on the ratio of reserved to total clusters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterPoolAutoscalerSpec defines the autoscaling policy
              for a ClusterPool.
            properties:
              maxReplicas:
                description: MaxReplicas is the maximum pool size the autoscaler will
                  scale up to.
                format: int32
                minimum: 1
                type: integer
              minReplicas:
                default: 2
                description: MinReplicas is the minimum pool size the autoscaler will
                  scale down to.
                format: int32
                minimum: 1
                type: integer
              targetRef:
                description: TargetRef references the ClusterPool to scale.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              utilizationThreshold:
                default: "0.70"
                description: UtilizationThreshold is the ratio of reserved/total above
                  which the pool scales up.
                type: string
            required:
            - maxReplicas
            - minReplicas
            - targetRef
            type: object
          status:
            description: ClusterPoolAutoscalerStatus reports the autoscaler's observed
              state.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentReplicas:
                description: CurrentReplicas is the current ClusterPool replica count.
                format: int32
                type: integer
              currentUtilization:
                description: CurrentUtilization is the last measured utilization ratio.
                type: string
              desiredReplicas:
                description: DesiredReplicas is what the autoscaler last computed.
                format: int32
                type: integer
              lastScaleTime:
                description: LastScaleTime is when the autoscaler last changed the
                  replica count.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
