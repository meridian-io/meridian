---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusters.meridian.io
spec:
  group: meridian.io
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.profile
      name: Profile
      type: string
    - jsonPath: .spec.clientId
      name: ClientID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.lastRotatedAt
      name: LastRotated
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster represents a single managed Trino cluster instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of a Cluster.
            properties:
              clientId:
                description: ClientID is set by the REST API when reserving this cluster.
                type: string
              image:
                description: Image is the Trino container image to use.
                type: string
              profile:
                description: Profile is the configuration profile used to create this
                  cluster.
                type: string
              reservationId:
                description: ReservationID is a unique ID for the reservation, used
                  for idempotency.
                type: string
              workers:
                default: 2
                description: Workers is the number of Trino worker nodes.
                format: int32
                type: integer
            required:
            - image
            - profile
            type: object
          status:
            description: ClusterStatus defines the observed state of a Cluster.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              coordinatorUrl:
                description: CoordinatorURL is the connection URL once the cluster
                  is ready.
                type: string
              idleAt:
                description: IdleAt is when the cluster last became idle.
                format: date-time
                type: string
              phase:
                description: Phase is the current lifecycle phase of the cluster.
                type: string
              ready:
                description: Ready indicates whether the cluster passed its health
                  check.
                type: boolean
              reservedAt:
                description: ReservedAt is when the cluster was last reserved.
                format: date-time
                type: string
              lastRotatedAt:
                description: LastRotatedAt is when credentials were last successfully rotated.
                format: date-time
                type: string
              rotationFailures:
                description: RotationFailures is the count of consecutive credential rotation failures.
                format: int32
                type: integer
              rotatingCatalog:
                description: RotatingCatalog is the catalog name currently being rotated.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
