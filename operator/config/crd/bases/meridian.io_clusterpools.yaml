---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterpools.meridian.io
spec:
  group: meridian.io
  names:
    kind: ClusterPool
    listKind: ClusterPoolList
    plural: clusterpools
    singular: clusterpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.reservedReplicas
      name: Reserved
      type: integer
    - jsonPath: .status.pendingReplicas
      name: Pending
      type: integer
    - jsonPath: .spec.workload
      name: Workload
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterPool manages a hot standby pool of Trino clusters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterPoolSpec defines the desired state of a ClusterPool.
            properties:
              replicas:
                default: 3
                description: Replicas is the desired number of standby (idle) clusters
                  in the pool.
                format: int32
                minimum: 1
                type: integer
              reservationTtl:
                default: 4h
                description: ReservationTTL is the maximum duration a cluster may
                  remain Reserved before being automatically released back to the
                  pool (e.g. after a client crash). Accepts Go duration strings such
                  as "4h" or "30m".
                type: string
              template:
                description: Template defines the profile used when creating new clusters.
                properties:
                  image:
                    description: Image is the Trino container image.
                    type: string
                  profile:
                    description: Profile is the named configuration profile for new
                      clusters.
                    type: string
                  workers:
                    default: 2
                    description: Workers is the default number of workers per cluster.
                    format: int32
                    type: integer
                required:
                - image
                - profile
                type: object
              workload:
                description: Workload tags all clusters in this pool with a workload type (e.g. "analytics", "etl"). Used to route reservations without specifying a cluster name or profile.
                type: string
              gateway:
                description: Gateway configures optional Trino Gateway integration. Clusters are automatically registered on Idle and deregistered on deletion.
                properties:
                  endpoint:
                    description: Endpoint is the Trino Gateway base URL (e.g. "http://trino-gateway:8080").
                    type: string
                  routingGroup:
                    description: RoutingGroup is the routing group for registered backends. Defaults to the pool workload, or "adhoc".
                    type: string
                required:
                - endpoint
                type: object
            required:
            - replicas
            - template
            type: object
          status:
            description: ClusterPoolStatus defines the observed state of a ClusterPool.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedReplicas:
                description: FailedReplicas is the number of clusters in Failed phase.
                format: int32
                type: integer
              pendingReplicas:
                description: PendingReplicas is the number of clusters being created.
                format: int32
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of clusters currently in
                  Idle phase.
                format: int32
                type: integer
              reservedReplicas:
                description: ReservedReplicas is the number of clusters currently
                  Reserved.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
